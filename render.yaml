services:
  - type: web
    name: ghoul-weather
    runtime: docker
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    region: frankfurt
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: OWM_API_KEY
        sync: false
